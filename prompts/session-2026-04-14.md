# Session 2026-04-14 — Cycles 51 & 52

---

## Cycle 52 — SOS Form: Validation, Image Upload, Text-Only (v4.23.0)

### Outcome
Closed three closed-beta blockers reported by David:
1. Disabled Send SOS button with no feedback → now reveals field errors on click and scrolls to the first problem.
2. JPEG/HEIC upload failures → MIME allowlist and magic-byte validator extended; uppercase extensions and iPhone HEIC now upload successfully.
3. Text-only SOS silently rejected → `SOSConfirmStep` no longer returns silently on empty description; uses visible validation + aria-disabled submit.

### Code changes
- **New:** `src/app/(main)/sos/create/upload-helper.ts` — `uploadSosPhotoWithRetry()` client wrapper with single-retry on transient server errors; classifies error codes.
- **Edit:** `src/app/actions/sos.ts` — `uploadSosMedia()` rewritten: case-insensitive MIME allowlist (`image/jpeg|jpg|pjpeg|png|webp|heic|heif` + sequence variants), 10 MB size check, `validateImageBytes()` magic-byte validation, detected MIME passed to R2 (not browser-reported). Returns discriminated `{ error, code }` union.
- **Edit:** `src/lib/media.ts` — `extFromContentType()` normalizes to lowercase and adds entries for `image/jpg`, `image/pjpeg`, `image/heic`, `image/heif`, `image/heic-sequence`, `image/heif-sequence`.
- **Edit:** `src/lib/security/file-validation.ts` — `validateImageBytes()` extended to recognize the ISO base-media "ftyp" box with HEIF brand codes (`heic`, `heix`, `hevc`, `mif1`, etc.). Reads 16 bytes instead of 12.
- **Edit:** `src/app/(main)/sos/create/page.tsx` — added `touched` state + `fieldRefs` map for category/title/description; inline `text-destructive` errors; aria-disabled submit that marks all touched + scrolls to first error on click; upload via `uploadSosPhotoWithRetry()`; inline upload error state; updated accept attribute to include HEIC/HEIF.
- **Edit:** `src/app/(main)/sos/create/components/SOSConfirmStep.tsx` — `descriptionError` derived state (empty / <20 chars / null); `descriptionRef` focused on invalid submit; upload via helper; upload error banner; aria-disabled submit; removed silent `if (!description.trim()) return`.
- **Edit:** `src/app/(main)/sos/create/components/SOSProcessingStep.tsx` — switched to `uploadSosPhotoWithRetry()` instead of direct `uploadSosMedia()` so the AI-analysis pipeline benefits from the retry too.

### Notes on the "garbled text" check
No literal quality-filter regex or AI gate exists in the server action. The effective blocker was the silent no-op submit in `SOSConfirmStep` plus the taxonomy-less text entry into the category search. Both are fixed by the validation-feedback work above.

### Validation
- `npx tsc --noEmit` → clean (one transient error from SOSProcessingStep fixed by re-routing to the helper)
- `npm run lint` → 0 errors (63 pre-existing warnings unrelated)
- `npm run build` → success
- `npm test` → 89/89 pass

### Version
[4.23.0] — CHANGELOG + CLAUDE.md "SOS Form Validation & Upload" section added.

---

# Session 2026-04-14 — Cycle 51: Feed Comment Replies & Owner Delete (v4.22.0)

## Outcome
Shipped Facebook-style 1-level-deep threaded replies on feed comments plus hard-delete permissions for post owners and admins.

## DB changes
- `ALTER TABLE feed_post_comments ADD COLUMN parent_comment_id uuid REFERENCES feed_post_comments(id) ON DELETE CASCADE`
- `CREATE INDEX idx_feed_post_comments_parent ON feed_post_comments(parent_comment_id) WHERE parent_comment_id IS NOT NULL`
- Applied via Supabase Management API.

## Code changes
- **New:** `src/app/actions/feed-comments.ts` — dedicated server actions file containing `FeedComment` / `CommentWithReplyCount` types and `getPostComments`, `getReplies`, `addComment`, `addReply`, `deleteComment`. Helper `hydrateComments()` resolves author/company rows.
- **Refactored:** `src/app/actions/feed-posts.ts` — removed the in-file `FeedComment` interface and all comment functions; added `export type { FeedComment } from '@/app/actions/feed-comments'` for back-compat. Removed unused `safeErrorMessage` import.
- **New:** `src/app/(main)/feed/components/CommentItem.tsx` — recursive `'use client'` component. Top-level comments render Reply button, View/Hide replies toggle, and the overflow menu. Replies (isReply=true) indent `pl-8`, hide Reply button, hide reply list.
- **New:** `src/app/(main)/feed/components/ReplyInput.tsx` — pre-fills `@ParentAuthor `, auto-focuses with caret at end, Escape cancels.
- **Rewritten:** `src/app/(main)/feed/components/CommentSection.tsx` — now requires `postAuthorId` prop, uses `CommentWithReplyCount[]` state, delegates row rendering to `CommentItem`, handles deletion callbacks for both top-level and reply deletes.
- **Edit:** `src/app/(main)/feed/components/CommentInput.tsx` — import path `@/app/actions/feed-comments`.
- **Edit:** `src/app/(main)/feed/components/FeedPost.tsx` — passes `postAuthorId={post.author.id}` to CommentSection.

## Permission model (hard delete)
`deleteComment()` allows if: comment author OR post owner OR `profiles.admin_role IN ('superadmin','moderator')`. Top-level deletes count replies first, hard-delete the row (cascade removes replies), then decrement `feed_posts.comments_count` by `1 + reply_count` (clamped at 0). Reply deletes decrement by 1.

## Depth enforcement
- UI: no Reply button renders on comments where `isReply=true`.
- Server: `addReply()` fetches the parent row and throws `'Replies to replies are not allowed'` if `parent.parent_comment_id !== null`.

## Validation
- `npx tsc --noEmit` → clean
- `npm run lint` → 0 errors (63 pre-existing warnings unrelated)
- `npm run build` → success
- `npm test` → 89/89 pass

## Version
[4.22.0] — CHANGELOG and CLAUDE.md "Social Feed: Comments" section updated.
