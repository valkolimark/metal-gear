# Cycle 60 — Vision pipeline consolidation: SOS + listing analyzer migrated

Migrated `/api/listings/analyze-image` and the SOS camera-first flow onto the shared `src/lib/vision-analysis/` pipeline introduced in Cycle 58. The legacy Claude-only path in `equipment-prompts.ts` is now unused and carries an `@deprecated` header for removal in Cycle 64+. Audit revealed two discrepancies from the prompt's expected surfaces (documented in `prompts/cycle-60-audit.md`): SOS has no server-side image-analysis call to migrate — analysis is client → analyzer route → user-confirmed → `createSosRequest` persists, so the migration target was the analyzer route itself; and the analyzer route ingests base64 while `analyzeEquipmentImages` expects URLs, resolved by writing incoming base64 to a temp R2 key (`tmp/analyze/{userId}/{uuid}.ext`), invoking the pipeline, and best-effort cleanup. Added `mode: 'snap-list' | 'sos' | 'listing-helper'` to `analyzeEquipmentImages`; mode injects a single framing sentence into the Claude prompt without changing the JSON schema. Built `src/lib/sos/vision-orchestrator.ts` as pure projection helpers (`projectAnalysisToSosFields`, `buildSosClarifyingQuestions`) — no domain imports — which the analyzer route folds into the existing `AIAnalysisResult` envelope when `mode === 'sos'`. Both clients (`SOSProcessingStep`, `AIImageCapture`) now pass their `mode` field; response shape preserved end-to-end. Extended Cycle 59's copy-rule sweep to `src/app/(main)/sos` + `src/components/sos`. Architecture grep on `src/lib/vision-analysis/` returns empty; SOS surface has no banned strings. typecheck/lint/build pass; vitest 130/130 (added 6 SOS-orchestrator + 3 mode-dispatch test cases); new `e2e/sos.spec.ts` covers analyzer auth gate + non-breaking input contract. Audit note removed before commit.
