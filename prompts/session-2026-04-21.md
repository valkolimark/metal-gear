# Cycle 59 — Manual-First Listing Creation; Snap & List demoted to Photo-to-Listing (experimental)

Reversed Cycle 58's decision to make `/listings/snap` the default listing creation entry point. `/listings/new` now renders the multi-step manual form by default; mobile bottom-nav `+`, `MobileMenuDrawer` "Post a Listing", header "Create Listing", "My Listings" CTA, and the `/listings/create` Single Listing tile all route to `/listings/new`. Added a small dismissable `PhotoToListingHint` card above the manual form that links to the preserved `/listings/snap` flow (also reachable via `/listings/new?mode=photo`). Rewrote every user-visible "AI" / "Snap & List" / "AI-Assisted" / "AI-Powered" string on the listing-creation surface to neutral "Photo-to-Listing" / "Draft" language — internal code identifiers (`SnapListBadge`, `snap_list_events`, `SNAP_LIST_QUOTA`, `/admin/snap-list-metrics`), pilot tables, server actions, and the shared `src/lib/vision-analysis/` pipeline are unchanged so Cycle 60 (SOS + image-analyzer migration) is unaffected. Pilot instrumentation continues at lower volume; `SnapListBadge` now renders "Photo-to-Listing draft" on listings with `ai_assisted = true`. Verified by typecheck, lint, build, full vitest suite (121 tests), updated `e2e/snap-list.spec.ts`, and the §5.9 banned-string grep returning zero hits.
